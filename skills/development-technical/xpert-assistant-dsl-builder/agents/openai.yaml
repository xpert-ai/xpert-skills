interface:
  display_name: "Xpert Assistant DSL Builder"
  short_description: "Map, build, validate, and diagnose every Xpert Assistant DSL layer"
  default_prompt: "Use $xpert-assistant-dsl-builder to inspect the current Xpert contract, map Assistant and Studio properties to their DSL paths, generate all delivery layers from one definition, and validate the resulting graph."
